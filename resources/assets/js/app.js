
/**
 * First we will load all of this project's JavaScript dependencies which
 * includes Vue and other libraries. It is a great starting point when
 * building robust, powerful web applications using Vue and Laravel.
 */

require('./bootstrap');
require('./helpers');

window.Vue = require('vue');

/**
 * Next, we will create a fresh Vue application instance and attach it to
 * the page. Then, you may begin adding components to this application
 * or customize the JavaScript scaffolding to fit your unique needs.
 */

Vue.component('content-list', require('./components/ContentListComponent.vue'));
Vue.component('content-actions', require('./components/ContentActionsComponent.vue'));
Vue.component('content-uploader', require('./components/ContentUploaderComponent.vue'));


/**
 * Here we require the vue components that are offered by our plugins.
 * This file is automatically generated using `php artisan mediagal:plugin-js`
 */
require('./plugins.js');

/**
 * And here we instantiate our main vue app.
 */
const app = new Vue({
    el: '#app'
});
